from __future__ import annotations

from .schema import Actor, Artifact, HandoffPoint, Step


def extract_actors(text: str) -> list[Actor]:
    """MVP stub: return empty until rules are added."""
    _ = text
    return []


def extract_steps(text: str) -> list[Step]:
    """MVP stub: return empty until rules are added."""
    _ = text
    return []


def extract_artifacts(text: str) -> list[Artifact]:
    """MVP stub: return empty until rules are added."""
    _ = text
    return []


def extract_handoff_points(text: str) -> list[HandoffPoint]:
    """MVP stub: return empty until rules are added."""
    _ = text
    return []
