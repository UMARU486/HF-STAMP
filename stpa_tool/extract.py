from __future__ import annotations

from .schema import ProcessModel


def extract_process_model(text: str) -> ProcessModel:
    """MVP stub: return empty process model until rules are added."""
    _ = text
    return ProcessModel()
